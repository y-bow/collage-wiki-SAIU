# Tutorial reference (not part of the live site)

This is the stock Docusaurus classic-template tutorial content
(`tutorial-basics/`, `tutorial-extras/`, `intro.mdx`), moved out of `docs/`
so it stops appearing in the site's sidebar and the "Docs" nav item can
point at real wiki content instead. Kept here for reference (e.g. if anyone
needs the original Docusaurus walkthrough), not linked from anywhere on the
live site.
